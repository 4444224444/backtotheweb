import React from 'react';

const EraCard = ({ styleType }) => {
    return (
      <eraCard className={`eraCard ${styleType}`}>
        <h1>여기다가내용</h1>
      </eraCard>
    );
  };

export default EraCard;